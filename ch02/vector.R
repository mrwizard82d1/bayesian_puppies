# Illustrate vectors
c( 2.718, 3.14, 1.414 )

# Item by item multiplication
c( 1, 2, 3 ) * c( 4, 5, 6 ) # 4, 10, 18

# Scalar multiplication "distributes" over all items
2 * c( 1, 2, 3 ) # 2, 4, 6

# Similarly, scalar addition "distributes"
2 + c( 1, 2, 3 ) # 3, 4, 5

